def is_number(string):
    return string.isnumeric()
def main():
    x = input('Enter number > ')
    if (is_number(x) == True):
        x = int(x)
        y = x/3
        print(y)
    else:
        print('not a number')
while True:
    main()
